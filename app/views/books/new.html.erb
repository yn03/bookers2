<h2>User info</h2>
<%= form_with model: @book_image do |f| %>
  <h4>画像</h4>
  <%= f.file_field :image, accept: "image/*" %>
  <h4>Title</h4>
  <%= f.text_field :shop_name %>
  <h4>Opinion</h4>
  <%= f.text_area :caption %>
  <%= f.submit '投稿' %>
<% end %>